# 02: Blink using a timer

This tutorial teaches how to flash a minimal working code to the STM8.

## Build and flash

Now, you can simply build with `make` and flash with `make flash` commands. You should see the onboard LED start blinking at the rate defined in main.c