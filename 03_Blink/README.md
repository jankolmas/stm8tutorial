# 03: Define blink functions

In this chapter, we define blink functions as a way to get feedback from the STM8

## Build and flash

See instructions in chapter 00 on how to set up the hardware and development environment. Build the program with `make` and flash with `make flash` commands. 

After startup, you should see the morse code for "STM".
